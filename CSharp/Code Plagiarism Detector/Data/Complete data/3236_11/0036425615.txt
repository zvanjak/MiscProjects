#include <stdio.h>
#include <conio.h>
#include <math.h>
int main ()
{int x,n,i;
float lnx;
 scanf ("%i,%i",&n,&x);
 if (n<1 || n>100)
   printf ("n nije valjan");
   return 0;
   else if (x<=0 || x>=2) 
   printf ("\nx nije valjan");
   return 0;
   else 
   printf ("\n%i ",n);
   printf ("%i ",x); 
   i=n;
   lnx=(x-1)-+((pow(x-1),i)/i;
   printf ("%f ",lnx);
 /*  getche ();*/
   return 0;
}

   